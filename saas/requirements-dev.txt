# Development & CI dependencies for the GVD SaaS service.
# Runtime deps are pinned in requirements.txt; these are tools only.

pytest==8.3.4
pytest-cov==6.0.0
ruff==0.9.2
pip-audit==2.7.3
cyclonedx-bom==5.1.1
